name: PR Reminder

on:
  schedule:
    - cron: '0 1 * * 1-5' # KST 오전 10시
  workflow_dispatch:

jobs:
  call-reusable-reminder:
    uses: 33Auto/.github/.github/workflows/reusable-pr-reminder.yml@main
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }} # 각 레포에 설정된 시크릿 사용
      inherit: true
